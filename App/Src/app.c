/**
 * @file    app.c
 * @brief   应用初始化, 按顺序初始化各 BSP 驱动模块
 * @note    App_Init() 在进入 FreeRTOS 调度器之前调用
 *          各任务入口函数由 CubeMX 在 freertos.c 中通过 osThreadNew() 注册
 */
#include "app.h"

/* TODO: 在此添加应用初始化 + 任务注册实现代码 */
